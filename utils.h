#ifndef UTIL
#define UTIL

#include <unistd.h>

int _putchar(char c);
int _strlen(char *str);
int _strcomp(char *str1, char *str2);
char *_strcpy(char *dest, const char *src);
int to_binary(unsigned int);

#endif

